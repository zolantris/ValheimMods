using UnityEngine;

namespace ValheimVehicles.WaterDisplacement
{
  public class TestScript : MonoBehaviour
  {
    // Start is called before the first frame update
    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }
  }
}